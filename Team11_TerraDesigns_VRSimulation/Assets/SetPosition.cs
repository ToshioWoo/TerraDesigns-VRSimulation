using UnityEngine;
using UnityEngine.SceneManagement;
using System.Collections;
using System.Collections.Generic;
using UnityEngine.XR.Interaction.Toolkit;

public class SetPosition : MonoBehaviour
{/*
    // Drag & drop the XR Origin GameObject here in the Inspector
    public XROrigin xrOrigin;

    void Start()
    {
        if (xrOrigin != null)
        {
            // Reset camera to world origin (0,0,0)
            xrOrigin.MoveCameraToWorldLocation(Vector3.zero);

            // Optional: Set tracking mode (e.g., Floor for standing)
            xrOrigin.RequestedTrackingOriginMode = TrackingOriginModeFlags.Floor;
        }
        else
        {
            Debug.LogError("XR Origin reference not assigned!");
        }
    }*/
}